
  # Adaptive Story Generation MVP (Copy)

  This is a code bundle for Adaptive Story Generation MVP (Copy). The original project is available at https://www.figma.com/design/UnKYG8uukjkMrxzD0Zu6rQ/Adaptive-Story-Generation-MVP--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  